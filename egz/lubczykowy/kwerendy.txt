kw1 - SELECT tytul, plik FROM `zdjecia` WHERE polubienia >=100;
kw2 - SELECT zdjecia.plik, zdjecia.tytul, zdjecia.polubienia, autorzy.imie, autorzy.nazwisko FROM zdjecia JOIN autorzy ON zdjecia.autorzy_id=autorzy.id ORDER BY autorzy.nazwisko ASC;
kw3 - SELECT autorzy.imie, COUNT(zdjecia.autorzy_id) FROM `autorzy` join zdjecia WHERE autorzy.id = zdjecia.autorzy_id group by zdjecia.autorzy_id;
kw4 - ALTER TABLE `zdjecia` ADD `rozmiarPliku` INT NOT NULL AFTER `polubienia`;